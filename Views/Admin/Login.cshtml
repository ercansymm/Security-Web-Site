@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Girişi - GuvenlikBul</title>

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --primary-blue: #3b82f6;
            --primary-indigo: #6366f1;
            --success-green: #10b981;
            --danger-red: #ef4444;
            --warning-orange: #f59e0b;
            --gray-50: #f8fafc;
            --gray-100: #f1f5f9;
            --gray-200: #e2e8f0;
            --gray-300: #cbd5e0;
            --gray-400: #94a3b8;
            --gray-500: #64748b;
            --gray-600: #475569;
            --gray-700: #334155;
            --gray-800: #1e293b;
            --gray-900: #0f172a;
            --radius-sm: 0.5rem;
            --radius-md: 0.75rem;
            --radius-lg: 1rem;
            --radius-xl: 1.5rem;
            --radius-2xl: 2rem;
            --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #667eea 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
            position: relative;
            overflow: hidden;
        }

            body::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: radial-gradient(circle at 20% 80%, rgba(120, 119, 198, 0.3) 0%, transparent 50%), radial-gradient(circle at 80% 20%, rgba(255, 255, 255, 0.1) 0%, transparent 50%), radial-gradient(circle at 40% 40%, rgba(120, 119, 198, 0.2) 0%, transparent 50%);
            }

        .login-wrapper {
            position: relative;
            z-index: 1;
            width: 100%;
            max-width: 450px;
        }

        .login-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: var(--radius-2xl);
            box-shadow: var(--shadow-xl);
            padding: 3rem;
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            position: relative;
            overflow: hidden;
        }

            .login-container::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 4px;
                background: linear-gradient(90deg, var(--primary-blue) 0%, var(--primary-indigo) 100%);
            }

        .login-header {
            text-align: center;
            margin-bottom: 2.5rem;
        }

        .brand-section {
            margin-bottom: 2rem;
        }

        .login-icon {
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-indigo) 100%);
            width: 5rem;
            height: 5rem;
            border-radius: var(--radius-xl);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
            position: relative;
        }

            .login-icon::after {
                content: '';
                position: absolute;
                top: -2px;
                left: -2px;
                right: -2px;
                bottom: -2px;
                background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.3), transparent);
                border-radius: var(--radius-xl);
                z-index: -1;
            }

            .login-icon i {
                font-size: 2rem;
                color: white;
            }

        .brand-title {
            color: var(--gray-900);
            font-weight: 700;
            font-size: 2rem;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-indigo) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .brand-subtitle {
            color: var(--gray-500);
            font-weight: 500;
            font-size: 1rem;
            margin-bottom: 0.5rem;
        }

        .security-note {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1rem;
            background: rgba(16, 185, 129, 0.1);
            color: var(--success-green);
            border-radius: var(--radius-lg);
            font-size: 0.875rem;
            font-weight: 500;
        }

        .login-form {
            margin-bottom: 2rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
            position: relative;
        }

        .form-label {
            font-weight: 500;
            color: var(--gray-700);
            margin-bottom: 0.75rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.875rem;
        }

            .form-label i {
                color: var(--gray-400);
            }

        .input-container {
            position: relative;
        }

        .form-control {
            width: 100%;
            border: 2px solid var(--gray-200);
            border-radius: var(--radius-lg);
            padding: 1rem 3rem 1rem 1rem;
            font-size: 1rem;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            background: white;
            color: var(--gray-900);
        }

            .form-control:focus {
                outline: none;
                border-color: var(--primary-blue);
                box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
                transform: translateY(-1px);
            }

            .form-control::placeholder {
                color: var(--gray-400);
            }

        .input-icon {
            position: absolute;
            right: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--gray-400);
            font-size: 1.125rem;
            transition: color 0.3s ease;
        }

        .form-group:focus-within .input-icon {
            color: var(--primary-blue);
        }

        .btn-login {
            width: 100%;
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-indigo) 100%);
            border: none;
            border-radius: var(--radius-lg);
            padding: 1rem 1.5rem;
            font-size: 1rem;
            font-weight: 600;
            color: white;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
            position: relative;
            overflow: hidden;
        }

            .btn-login::before {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 100%;
                height: 100%;
                background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
                transition: left 0.6s ease;
            }

            .btn-login:hover {
                transform: translateY(-2px);
                box-shadow: 0 8px 25px rgba(59, 130, 246, 0.5);
            }

                .btn-login:hover::before {
                    left: 100%;
                }

            .btn-login:active {
                transform: translateY(0);
            }

            .btn-login:disabled {
                opacity: 0.7;
                cursor: not-allowed;
                transform: none;
            }

        .error-alert {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.2);
            border-left: 4px solid var(--danger-red);
            border-radius: var(--radius-lg);
            padding: 1rem 1.25rem;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

            .error-alert i {
                color: var(--danger-red);
                font-size: 1.125rem;
            }

        .error-alert-text {
            color: #991b1b;
            font-size: 0.875rem;
            font-weight: 500;
            margin: 0;
        }

        .login-footer {
            text-align: center;
            padding-top: 1.5rem;
            border-top: 1px solid var(--gray-200);
        }

        .footer-text {
            color: var(--gray-500);
            font-size: 0.875rem;
            margin: 0;
        }

        .security-features {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 1rem;
        }

        .security-feature {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--gray-400);
            font-size: 0.75rem;
        }

            .security-feature i {
                color: var(--success-green);
            }

        /* Loading State */
        .form-loading {
            opacity: 0.7;
            pointer-events: none;
        }

        .btn-spinner {
            width: 1rem;
            height: 1rem;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-top: 2px solid white;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        /* Input Validation States */
        .form-control.is-invalid {
            border-color: var(--danger-red);
            box-shadow: 0 0 0 3px rgba(239, 68, 68, 0.1);
        }

        .form-control.is-valid {
            border-color: var(--success-green);
            box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
        }

        .validation-feedback {
            font-size: 0.875rem;
            margin-top: 0.5rem;
            padding-left: 0.5rem;
        }

        .invalid-feedback {
            color: var(--danger-red);
        }

        .valid-feedback {
            color: var(--success-green);
        }

        /* Focus styles for accessibility */
        .form-control:focus,
        .btn-login:focus {
            outline: 2px solid var(--primary-blue);
            outline-offset: 2px;
        }



        /* Spin animasyonu */
        @@keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        /* Tablet ve küçük laptop için (1024px ve altı) */
        @@media (max-width: 1024px) {
            body {
                padding: 0.75rem;
            }

            .login-wrapper {
                max-width: 420px;
            }

            .login-container {
                padding: 2.5rem;
            }

            .login-icon {
                width: 4.5rem;
                height: 4.5rem;
                margin-bottom: 1.25rem;
            }

                .login-icon i {
                    font-size: 1.75rem;
                }

            .brand-title {
                font-size: 1.875rem;
            }

            .brand-subtitle {
                font-size: 0.95rem;
            }

            .security-note {
                font-size: 0.8rem;
                padding: 0.4rem 0.8rem;
            }

            .form-control {
                padding: 0.875rem 2.75rem 0.875rem 0.875rem;
                font-size: 0.95rem;
            }

            .btn-login {
                padding: 0.875rem 1.25rem;
                font-size: 0.95rem;
            }

            .security-features {
                gap: 1.5rem;
                flex-wrap: wrap;
            }

            .security-feature {
                font-size: 0.7rem;
            }
        }

        /* Tablet için (768px ve altı) */
        @@media (max-width: 768px) {
            body {
                padding: 0.5rem;
                align-items: flex-start;
                padding-top: 2rem;
            }

            .login-wrapper {
                max-width: 100%;
                width: 100%;
                max-width: 400px;
            }

            .login-container {
                padding: 2rem;
                border-radius: var(--radius-xl);
            }

            .login-header {
                margin-bottom: 2rem;
            }

            .brand-section {
                margin-bottom: 1.5rem;
            }

            .login-icon {
                width: 4rem;
                height: 4rem;
                margin-bottom: 1rem;
            }

                .login-icon i {
                    font-size: 1.5rem;
                }

            .brand-title {
                font-size: 1.75rem;
                margin-bottom: 0.375rem;
            }

            .brand-subtitle {
                font-size: 0.9rem;
                margin-bottom: 0.75rem;
            }

            .security-note {
                font-size: 0.75rem;
                padding: 0.375rem 0.75rem;
                gap: 0.375rem;
            }

            .login-form {
                margin-bottom: 1.5rem;
            }

            .form-group {
                margin-bottom: 1.25rem;
            }

            .form-label {
                margin-bottom: 0.5rem;
                font-size: 0.8rem;
                gap: 0.375rem;
            }

            .form-control {
                padding: 0.75rem 2.5rem 0.75rem 0.75rem;
                font-size: 0.9rem;
                border-radius: var(--radius-md);
            }

            .input-icon {
                right: 0.75rem;
                font-size: 1rem;
            }

            .btn-login {
                padding: 0.75rem 1rem;
                font-size: 0.9rem;
                gap: 0.5rem;
                border-radius: var(--radius-md);
            }

            .error-alert {
                padding: 0.75rem 1rem;
                margin-bottom: 1.25rem;
                gap: 0.5rem;
            }

                .error-alert i {
                    font-size: 1rem;
                }

            .error-alert-text {
                font-size: 0.8rem;
            }

            .login-footer {
                padding-top: 1.25rem;
            }

            .security-features {
                gap: 1rem;
                justify-content: center;
                flex-wrap: wrap;
            }

            .security-feature {
                font-size: 0.65rem;
                gap: 0.375rem;
            }

            .footer-text {
                font-size: 0.8rem;
                margin-top: 0.75rem;
            }

            .validation-feedback {
                font-size: 0.8rem;
                margin-top: 0.375rem;
            }
        }

        /* Büyük telefon için (640px ve altı) */
        @@media (max-width: 640px) {
            body {
                padding: 0.375rem;
                padding-top: 1.5rem;
            }

            .login-wrapper {
                max-width: 360px;
            }

            .login-container {
                padding: 1.75rem;
            }

            .login-header {
                margin-bottom: 1.75rem;
            }

            .brand-section {
                margin-bottom: 1.25rem;
            }

            .login-icon {
                width: 3.5rem;
                height: 3.5rem;
                margin-bottom: 0.875rem;
            }

                .login-icon i {
                    font-size: 1.375rem;
                }

            .brand-title {
                font-size: 1.5rem;
                margin-bottom: 0.25rem;
            }

            .brand-subtitle {
                font-size: 0.85rem;
                margin-bottom: 0.625rem;
            }

            .security-note {
                font-size: 0.7rem;
                padding: 0.3rem 0.6rem;
            }

            .form-group {
                margin-bottom: 1rem;
            }

            .form-label {
                font-size: 0.75rem;
                margin-bottom: 0.375rem;
            }

            .form-control {
                padding: 0.625rem 2.25rem 0.625rem 0.625rem;
                font-size: 0.85rem;
            }

            .input-icon {
                right: 0.625rem;
                font-size: 0.95rem;
            }

            .btn-login {
                padding: 0.625rem 0.875rem;
                font-size: 0.85rem;
                gap: 0.375rem;
            }

            .error-alert {
                padding: 0.625rem 0.875rem;
                margin-bottom: 1rem;
            }

                .error-alert i {
                    font-size: 0.95rem;
                }

            .error-alert-text {
                font-size: 0.75rem;
            }

            .login-footer {
                padding-top: 1rem;
            }

            .security-features {
                gap: 0.75rem;
                margin-top: 0.75rem;
            }

            .security-feature {
                font-size: 0.6rem;
            }

            .footer-text {
                font-size: 0.75rem;
                margin-top: 0.625rem;
            }
        }

        /* Küçük telefon için (480px ve altı) */
        @@media (max-width: 480px) {
            body {
                padding: 0.25rem;
                padding-top: 1rem;
            }

            .login-wrapper {
                max-width: 320px;
            }

            .login-container {
                padding: 1.5rem;
            }

            .login-header {
                margin-bottom: 1.5rem;
            }

            .brand-section {
                margin-bottom: 1rem;
            }

            .login-icon {
                width: 3rem;
                height: 3rem;
                margin-bottom: 0.75rem;
            }

                .login-icon i {
                    font-size: 1.25rem;
                }

            .brand-title {
                font-size: 1.375rem;
                margin-bottom: 0.25rem;
            }

            .brand-subtitle {
                font-size: 0.8rem;
                margin-bottom: 0.5rem;
            }

            .security-note {
                font-size: 0.65rem;
                padding: 0.25rem 0.5rem;
                gap: 0.25rem;
            }

            .login-form {
                margin-bottom: 1.25rem;
            }

            .form-group {
                margin-bottom: 0.875rem;
            }

            .form-label {
                font-size: 0.7rem;
                margin-bottom: 0.25rem;
                gap: 0.25rem;
            }

            .form-control {
                padding: 0.5rem 2rem 0.5rem 0.5rem;
                font-size: 0.8rem;
            }

            .input-icon {
                right: 0.5rem;
                font-size: 0.9rem;
            }

            .btn-login {
                padding: 0.5rem 0.75rem;
                font-size: 0.8rem;
                gap: 0.25rem;
                min-height: 44px; /* Touch friendly */
            }

            .error-alert {
                padding: 0.5rem 0.75rem;
                margin-bottom: 0.875rem;
                gap: 0.375rem;
            }

                .error-alert i {
                    font-size: 0.9rem;
                }

            .error-alert-text {
                font-size: 0.7rem;
                line-height: 1.4;
            }

            .login-footer {
                padding-top: 0.875rem;
            }

            .security-features {
                flex-direction: column;
                gap: 0.5rem;
                margin-top: 0.625rem;
            }

            .security-feature {
                font-size: 0.55rem;
                justify-content: center;
            }

            .footer-text {
                font-size: 0.7rem;
                margin-top: 0.5rem;
                line-height: 1.4;
            }

            .validation-feedback {
                font-size: 0.7rem;
                margin-top: 0.25rem;
            }
        }

        /* Çok küçük telefon için (360px ve altı) */
        @@media (max-width: 360px) {
            body {
                padding: 0.125rem;
                padding-top: 0.75rem;
            }

            .login-wrapper {
                max-width: 280px;
            }

            .login-container {
                padding: 1.25rem;
            }

            .login-icon {
                width: 2.5rem;
                height: 2.5rem;
                margin-bottom: 0.625rem;
            }

                .login-icon i {
                    font-size: 1.125rem;
                }

            .brand-title {
                font-size: 1.25rem;
            }

            .brand-subtitle {
                font-size: 0.75rem;
            }

            .security-note {
                font-size: 0.6rem;
                padding: 0.2rem 0.4rem;
            }

            .form-control {
                padding: 0.4rem 1.75rem 0.4rem 0.4rem;
                font-size: 0.75rem;
            }

            .input-icon {
                right: 0.4rem;
                font-size: 0.85rem;
            }

            .btn-login {
                padding: 0.4rem 0.6rem;
                font-size: 0.75rem;
                min-height: 42px;
            }

            .error-alert {
                padding: 0.4rem 0.6rem;
                gap: 0.25rem;
            }

            .error-alert-text {
                font-size: 0.65rem;
            }

            .security-feature {
                font-size: 0.5rem;
            }

            .footer-text {
                font-size: 0.65rem;
            }
        }

        /* Landscape phone için özel ayarlar */
        @@media (max-height: 600px) and (orientation: landscape) {
            body {
                padding-top: 0.5rem;
                padding-bottom: 0.5rem;
            }

            .login-container {
                padding: 1.5rem;
            }

            .login-header {
                margin-bottom: 1rem;
            }

            .brand-section {
                margin-bottom: 0.75rem;
            }

            .login-icon {
                width: 2.5rem;
                height: 2.5rem;
                margin-bottom: 0.5rem;
            }

            .brand-title {
                font-size: 1.25rem;
                margin-bottom: 0.125rem;
            }

            .brand-subtitle {
                font-size: 0.75rem;
                margin-bottom: 0.375rem;
            }

            .security-note {
                font-size: 0.65rem;
                padding: 0.25rem 0.5rem;
            }

            .form-group {
                margin-bottom: 0.75rem;
            }

            .login-form {
                margin-bottom: 1rem;
            }

            .login-footer {
                padding-top: 0.75rem;
            }

            .security-features {
                margin-top: 0.5rem;
                gap: 0.5rem;
            }

            .footer-text {
                margin-top: 0.375rem;
            }
        }

        /* Touch optimizasyonu */
        @@media (hover: none) and (pointer: coarse) {
            .btn-login:hover,
            .form-control:focus {
                transform: none;
            }

            .btn-login:active {
                transform: scale(0.98);
            }

            .form-control:focus {
                transform: none;
            }
            /* Touch için daha büyük dokunma alanları */
            .form-control {
                min-height: 48px;
                padding: 0.75rem 2.5rem 0.75rem 0.75rem;
            }

            .btn-login {
                min-height: 48px;
                padding: 0.75rem 1rem;
            }

            .password-toggle {
                min-width: 44px !important;
                min-height: 44px !important;
                display: flex !important;
                align-items: center !important;
                justify-content: center !important;
            }

            .input-icon {
                min-width: 44px;
                min-height: 44px;
                display: flex;
                align-items: center;
                justify-content: center;
                right: 2px;
                top: 2px;
                transform: none;
                height: calc(100% - 4px);
            }
        }

        /* Print media query */
        @@media print {
            body {
                background: white;
                padding: 1rem;
            }

                body::before {
                    display: none;
                }

            .login-container {
                box-shadow: none;
                border: 1px solid #ccc;
                background: white;
            }

                .login-container::before {
                    background: #333;
                }

            .login-icon {
                background: #333 !important;
                box-shadow: none;
            }

            .brand-title {
                color: #000 !important;
                -webkit-text-fill-color: #000 !important;
            }

            .btn-login,
            .security-note,
            .error-alert {
                background: white !important;
                color: #000 !important;
                border: 1px solid #ccc;
            }

            .security-features,
            .password-toggle {
                display: none;
            }
        }

        /* Animasyonlar - Keyframes */
        @@keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @@keyframes slideInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @@keyframes scaleIn {
            from {
                transform: scale(0.9);
                opacity: 0;
            }

            to {
                transform: scale(1);
                opacity: 1;
            }
        }

        @@keyframes glow {
            0%, 100% {
                box-shadow: 0 0 5px rgba(59, 130, 246, 0.5);
            }

            50% {
                box-shadow: 0 0 20px rgba(59, 130, 246, 0.8);
            }
        }

        /* Animasyon sınıfları */
        .fade-in-up {
            animation: fadeInUp 0.8s ease-out;
        }

        .slide-in-down {
            animation: slideInDown 0.6s ease-out;
        }

        .scale-in {
            animation: scaleIn 0.4s ease-out;
        }

        .glow-animation {
            animation: glow 2s infinite;
        }

        /* Login container animasyonu */
        .login-container {
            animation: scaleIn 0.6s ease-out;
        }

        .login-icon {
            animation: bounceIn 0.8s ease-out 0.3s both;
        }

        @@keyframes bounceIn {
            0% {
                transform: scale(0.3);
                opacity: 0;
            }

            50% {
                transform: scale(1.05);
            }

            70% {
                transform: scale(0.9);
            }

            100% {
                transform: scale(1);
                opacity: 1;
            }
        }

        .brand-title {
            animation: slideInDown 0.6s ease-out 0.5s both;
        }

        .login-form {
            animation: fadeInUp 0.6s ease-out 0.7s both;
        }

        .login-footer {
            animation: fadeInUp 0.6s ease-out 0.9s both;
        }

        /* Reduced motion için */
        @@media (prefers-reduced-motion: reduce) {
            *,
            *::before,
            *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }

            .btn-login:hover,
            .form-control:focus {
                transform: none;
            }

            .btn-spinner {
                animation: none;
                border: 2px solid rgba(255, 255, 255, 0.3);
                border-top-color: white;
            }

            .login-container,
            .login-icon,
            .brand-title,
            .login-form,
            .login-footer {
                animation: none;
            }
        }

        /* Focus styles mobile için iyileştirme */
        @@media (max-width: 768px) {
            .form-control:focus,
            .btn-login:focus {
                outline: 2px solid var(--primary-blue);
                outline-offset: 2px;
            }

            .password-toggle:focus {
                outline: 2px solid var(--primary-blue);
                outline-offset: 2px;
            }
        }

        /* Özel mobil düzenlemeler */
        @@media (max-width: 640px) {
            /* Form group spacing */
            .form-group:last-of-type {
                margin-bottom: 1.25rem;
            }
            /* Security features mobil düzen */
            .security-features {
                justify-content: space-around;
            }
            /* Error alert mobil */
            .error-alert {
                text-align: left;
                border-radius: var(--radius-md);
            }
            /* Login button mobil */
            .btn-login {
                width: 100%;
                justify-content: center;
            }
            /* Input container mobil */
            .input-container {
                position: relative;
            }

            .password-toggle {
                position: absolute !important;
                right: 0 !important;
                top: 0 !important;
                height: 100% !important;
                width: 44px !important;
                display: flex !important;
                align-items: center !important;
                justify-content: center !important;
            }
        }

        /* Very small screens için özel */
        @@media (max-width: 320px) {
            .brand-title {
                font-size: 1.125rem;
            }

            .brand-subtitle {
                font-size: 0.7rem;
            }

            .form-control {
                font-size: 0.7rem;
            }

            .btn-login {
                font-size: 0.7rem;
            }

            .footer-text {
                font-size: 0.6rem;
            }
        }
    </style>

</head>
<body>
    <div class="login-wrapper">
        <div class="login-container">
            <div class="login-header">
                <div class="brand-section">
                    <div class="login-icon">
                        <i class="bi bi-shield-lock"></i>
                    </div>
                    <h1 class="brand-title">GuvenlikBul</h1>
                    <p class="brand-subtitle">Admin Panel Girişi</p>
                    <div class="security-note">
                        <i class="bi bi-shield-check"></i>
                        <span>Güvenli Giriş</span>
                    </div>
                </div>
            </div>

            <form id="loginForm" method="post" asp-action="Login" asp-controller="Admin" class="login-form">
                <!-- Error Alert -->
                @if (ViewBag.Error != null)
                {
                    <div class="error-alert">
                        <i class="bi bi-exclamation-triangle-fill"></i>
                        <div class="error-alert-text">@ViewBag.Error</div>
                    </div>
                }

                <!-- Username Field -->
                <div class="form-group">
                    <label for="username" class="form-label">
                        <i class="bi bi-person"></i>
                        Kullanıcı Adı
                    </label>
                    <div class="input-container">
                        <input type="text"
                               class="form-control"
                               id="username"
                               name="UserName"
                               placeholder="Kullanıcı adınızı girin"
                               required
                               autocomplete="username">
                        <i class="input-icon bi bi-person"></i>
                    </div>
                    <div class="invalid-feedback validation-feedback"></div>
                </div>

                <!-- Password Field -->
                <div class="form-group">
                    <label for="password" class="form-label">
                        <i class="bi bi-lock"></i>
                        Şifre
                    </label>
                    <div class="input-container">
                        <input type="password"
                               class="form-control"
                               id="password"
                               name="Password"
                               placeholder="Şifrenizi girin"
                               required
                               autocomplete="current-password">
                        <button type="button"
                                class="input-icon password-toggle"
                                onclick="togglePassword()"
                                title="Şifreyi göster/gizle">
                            <i class="bi bi-eye" id="passwordToggleIcon"></i>
                        </button>
                    </div>
                    <div class="invalid-feedback validation-feedback"></div>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="btn-login" id="loginButton">
                    <i class="bi bi-box-arrow-in-right" id="loginIcon"></i>
                    <span id="loginText">Giriş Yap</span>
                </button>
            </form>

            <div class="login-footer">
                <div class="security-features">
                    <div class="security-feature">
                        <i class="bi bi-shield-check"></i>
                        <span>SSL Şifreleme</span>
                    </div>
                    <div class="security-feature">
                        <i class="bi bi-lock"></i>
                        <span>Güvenli Oturum</span>
                    </div>
                    <div class="security-feature">
                        <i class="bi bi-eye-slash"></i>
                        <span>Gizlilik</span>
                    </div>
                </div>
                <p class="footer-text">&copy; 2025 GuvenlikBul - Tüm hakları saklıdır</p>
            </div>
        </div>
    </div>

    <script>
        // Form validation and submission
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            const form = this;
            const loginButton = document.getElementById('loginButton');
            const loginIcon = document.getElementById('loginIcon');
            const loginText = document.getElementById('loginText');

            // Validate inputs
            const username = document.getElementById('username');
            const password = document.getElementById('password');

            let isValid = true;

            // Clear previous validation
            clearValidation();

            // Username validation
            if (!username.value.trim()) {
                setInvalid(username, 'Kullanıcı adı gerekli');
                isValid = false;
            } else if (username.value.length < 3) {
                setInvalid(username, 'Kullanıcı adı en az 3 karakter olmalı');
                isValid = false;
            } else {
                setValid(username);
            }

            // Password validation
            if (!password.value.trim()) {
                setInvalid(password, 'Şifre gerekli');
                isValid = false;
            } else {
                setValid(password);
            }

            if (!isValid) {
                e.preventDefault();
                return false;
            }

            // Add loading state
            form.classList.add('form-loading');
            loginButton.disabled = true;
            loginIcon.className = '';
            loginIcon.innerHTML = '<div class="btn-spinner"></div>';
            loginText.textContent = 'Giriş yapılıyor...';
        });

        function clearValidation() {
            document.querySelectorAll('.form-control').forEach(input => {
                input.classList.remove('is-invalid', 'is-valid');
            });
            document.querySelectorAll('.validation-feedback').forEach(feedback => {
                feedback.textContent = '';
            });
        }

        function setInvalid(input, message) {
            input.classList.add('is-invalid');
            input.classList.remove('is-valid');
            const feedback = input.closest('.form-group').querySelector('.invalid-feedback');
            if (feedback) {
                feedback.textContent = message;
            }
        }

        function setValid(input) {
            input.classList.add('is-valid');
            input.classList.remove('is-invalid');
        }

        // Password visibility toggle
        function togglePassword() {
            const passwordInput = document.getElementById('password');
            const toggleIcon = document.getElementById('passwordToggleIcon');

            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                toggleIcon.className = 'bi bi-eye-slash';
            } else {
                passwordInput.type = 'password';
                toggleIcon.className = 'bi bi-eye';
            }
        }

        // Style password toggle button
        document.querySelector('.password-toggle').style.cssText = `
            background: none;
            border: none;
            color: var(--gray-400);
            cursor: pointer;
            padding: 0;
            width: auto;
            height: auto;
            transition: color 0.3s ease;
        `;

        // Enhanced input interactions
        document.querySelectorAll('.form-control').forEach(input => {
            input.addEventListener('focus', function() {
                this.closest('.form-group').classList.add('focused');
            });

            input.addEventListener('blur', function() {
                this.closest('.form-group').classList.remove('focused');
            });

            // Real-time validation feedback
            input.addEventListener('input', function() {
                if (this.classList.contains('is-invalid')) {
                    this.classList.remove('is-invalid');
                }
            });
        });

        // Keyboard shortcuts
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Enter' && e.ctrlKey) {
                document.getElementById('loginForm').dispatchEvent(new Event('submit'));
            }
        });

        // Auto-focus username on load
        window.addEventListener('load', function() {
            document.getElementById('username').focus();
        });
    </script>
</body>
</html>